{
  "schemaValid": true,
  "retriable": false,
  "wireMatchesCatalog": true,
  "note": "Schema validation runs BEFORE signature verification, so this is the rejection a malformed command gets even when its signature would have verified."
}
